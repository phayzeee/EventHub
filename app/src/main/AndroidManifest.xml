<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eventhub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EventHub">
        <activity android:name=".AdminView">
        </activity>
        <activity android:name=".CustomerInfo" />
        <activity android:name=".Admin" />
        <activity android:name=".PhotoRecycler" />
        <activity android:name=".DecoratorRecycler" />
        <activity android:name=".CatererRecycler" />
        <activity android:name=".AddToCart" />
        <activity android:name=".VenueRecycler" />
        <activity
            android:name=".Customer"
            android:label="Home" />
        <activity android:name=".Login" />
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>